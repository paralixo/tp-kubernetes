apiVersion: v1
kind: Namespace
metadata:
  name: bdd

---

apiVersion: v1
kind: ResourceQuota
metadata:
  name: quota-bdd
  namespace: bdd
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 2Gi
    limits.cpu: "4"
    limits.memory: 4Gi